

## Remove UrgencyBadge Dead Code

**File:** `src/components/PowerToolDemo.tsx`

Delete lines 36-72 (the comment block and the entire `UrgencyBadge` component definition). The component is no longer referenced anywhere after the previous removal of its usage.

