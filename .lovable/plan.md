

## Plan: Wire Download All and Share Buttons

### File: `src/components/EvidenceLocker.tsx`

**1. Add import**
```typescript
import { useToast } from "@/hooks/use-toast";
```

**2. Add `generateReportText()` helper** (inside the component, using props)

Produces a formatted plain-text report:

```text
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  WINDOWMAN QUOTE ANALYSIS REPORT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  Grade:              C
  Location:           Broward County
  Savings Opportunity: $4,800

── RED FLAG ANALYSIS ─────────────
  • 1 critical flag(s)
  • 2 caution flag(s)
  • 1 confirmed item(s)

── NEGOTIATION SCRIPT ────────────
  1. Ask for the window brand in writing
  2. Request a 3-year warranty minimum
  3. Mention you have a competing quote
  4. Ask for itemized material vs labor costs

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  Generated by WindowMan
  windowman.com
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**3. Wire "Download All" button** (line 147)

Replace the `console.log` only handler with:
- Call `generateReportText()`
- Create `Blob` with `text/plain` type
- Create temporary `<a>` with `download="WindowMan-Report.txt"`, click it, revoke URL
- Show toast: `{ title: "Success", description: "Report downloaded to your device" }`
- Keep existing analytics log

**4. Wire "Share" button** (line 153)

Replace the `console.log` only handler with:
- Call `generateReportText()`
- If `navigator.share` exists: call `navigator.share({ title: "WindowMan Quote Analysis", text })` 
- Else: call `navigator.clipboard.writeText(text)` and show toast `{ title: "Copied", description: "Report copied to clipboard" }`
- Keep existing analytics log

### No other files changed.

